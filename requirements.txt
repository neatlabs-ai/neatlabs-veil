# VEIL 3.0 — Network Traffic Exposer
# NEATLABS Intelligence Technology

# Required
PyQt6>=6.5.0

# Recommended
PyQt6-WebEngine>=6.5.0    # 3D globe visualization
psutil>=5.9.0             # Process mapping + fallback capture

# Optional
scapy>=2.5.0              # Full packet capture (needs admin/root)
openai==0.28              # AI-powered traffic analysis (GPT-4.1-nano)
